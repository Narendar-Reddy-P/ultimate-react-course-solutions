//creating fake server using "npm i json-server" add server running commands to script
//create react app using .js files instead of .jsx file for componentts
//to use images in img in component , first import the images
////Auto rename extension is helpful in renaming components
// snippet rfc to create components fast used by jonas
// future features, 1. allow users to use diffulty lvl of questions 2. store all user entered answers in an array and allow users to move back and forth
3.store users highscore in fake api and show it to user even after reloads
