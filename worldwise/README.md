//<a> reloads page
// <Link> doesn't reloads page
// here we should write "/pricing" only not "pricing"
// in class modules "element selectors" ul{} can't be used only class and id selectors. element selectors do work but they cause errors by selecting all elements in the document. but ".className element{}" can be used.
// class modules take classes created by us and so random id so that it doesn't matches with any other similar classes in other components like ".nav"=>".nav_hqhq_1"
// :global(.test){} global selecter in css modules nav :global(.active) {
background-color: green;
}
//assests folder directly imported to the js code
